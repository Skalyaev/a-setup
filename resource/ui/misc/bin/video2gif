#!/bin/bash
YELLOW='\033[0;33m'
BLUE='\033[0;34m'
GRAY='\033[0;37m'
NC='\033[0m'

USAGE="$GRAY===================usage$NC
$YELLOW$(basename "$0") $BLUE<path> <fps>$NC

Convert video to gif
"
(( "$#" != 2 )) && echo -e "$USAGE" && exit 1

ffmpeg -i "$1"\
    -r "$2"\
    "${1%.*}.gif"
