vim @ https://github.com/vim/vim.git ~ Éditeur de texte
#### /tmp/vim
$- INSTALL
git clone $src $dst
cd $dst
sed -i 's/#CONF_OPT_PYTHON = --enable-pythoninterp/CONF_OPT_PYTHON = --enable-pythoninterp/g' src/Makefile
sed -i 's/#CONF_OPT_PYTHON3 = --enable-pythoninterp/CONF_OPT_PYTHON3 = --enable-pythoninterp/g' src/Makefile
if vim --version; then make distclean; fi
./configure --prefix=$HOME/.local
make
make install DESTDIR=$HOME/.local
curl -fLo $HOME/.vim/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
$---
$- UPDATE
cd $dst
git pull
sed -i 's/#CONF_OPT_PYTHON = --enable-pythoninterp/CONF_OPT_PYTHON = --enable-pythoninterp/g' src/Makefile
sed -i 's/#CONF_OPT_PYTHON3 = --enable-pythoninterp/CONF_OPT_PYTHON3 = --enable-pythoninterp/g' src/Makefile
if vim --version; then make distclean; fi
./configure --prefix=$HOME/.local
make
make install DESTDIR=$HOME/.local
$---
$- REMOVE
cd $dst
make uninstall_runtime
cd
rm -rf $dst
$---
@@@@
