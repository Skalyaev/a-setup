#
#
#================================== UI GROUP - GUI
do_i3() {
    case $COMMAND in
    install) ;;
    restore) ;;
    esac
}

do_picom() {
    case $COMMAND in
    install) ;;
    restore) ;;
    esac
}

do_lightdm() {
    case $COMMAND in
    install) ;;
    restore) ;;
    esac
}
